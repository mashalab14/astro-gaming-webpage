---
// Gamesurface Footer component - Purple area with control buttons (3rem height)
---

<footer class="gamesurface-footer">
  <!-- Game Controls -->
  <div class="game-controls">
    <button id="newDealBtn" class="control-btn" type="button" aria-label="New Deal">New Deal</button>
    <button id="undoBtn" class="control-btn" type="button" aria-label="Undo">Undo</button>
    <button id="hintBtn" class="control-btn" type="button" aria-label="Hint">Hint</button>
    <button id="rulesBtn" class="control-btn" type="button" aria-label="Rules">Rules</button>
    <button id="moreBtn" class="control-btn" type="button" aria-label="More">More</button>
  </div>
</footer>

<style>
  /* ===== Gamesurface Footer (Purple - 3rem height) ===== */
  .gamesurface-footer {
    position: relative;
    height: 3rem;
    width: 100%;
    background: transparent;
    display: flex;
    align-items: center;
    z-index: 10;
    overflow: hidden;
  }

  /* ===== Game Control Buttons ===== */
  .game-controls {
    display: flex;
    gap: 12px;
    padding-left: 16px;
    align-items: center;
    position: relative;
    z-index: 15;
  }
  
  .control-btn {
    padding: 8px 16px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 6px;
    background: color-mix(in srgb, var(--table-bg) 70%, transparent);
    color: #e6e7eb;
    cursor: pointer;
    font-size: 16px;
    font-weight: 500;
    min-width: 80px;
    backdrop-filter: blur(4px);
    height: 2rem;
  }
  
  .control-btn:hover {
    background: color-mix(in srgb, var(--table-bg) 80%, transparent);
    border-color: rgba(255, 255, 255, 0.3);
  }
  
  .control-btn:focus-visible {
    outline: 2px solid #fff;
    outline-offset: 2px;
  }
  
  .control-btn:active {
    transform: translateY(1px);
  }


</style>
