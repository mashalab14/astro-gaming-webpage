---
// Ad Area component - 20% width reserved space for advertisements
---

<div class="ad-area" id="adOverlay">
  <button class="close-ad" onclick="document.getElementById('adOverlay').style.display='none'" aria-label="Close Ad">×</button>
  <div class="ad-placeholder">Ad Overlay<br><small>280×350px</small></div>
</div>

<style>
  /* ===== Ad Area (overlay) ===== */
  .ad-area {
    position: absolute;
    top: 5rem; /* Below the settings button */
    right: 1rem;
    width: 280px;
    height: 350px;
    background: color-mix(in srgb, var(--table-bg) 85%, transparent);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 8px;
    backdrop-filter: blur(8px);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 3; /* Below settings button but above game elements */
    box-sizing: border-box;
  }

  .ad-placeholder {
    color: #e6e7eb;
    font-size: 16px;
    font-weight: 600;
    text-align: center;
    opacity: 0.8;
    line-height: 1.4;
  }

  .ad-placeholder small {
    display: block;
    font-size: 12px;
    color: #9aa3ad;
    margin-top: 4px;
    font-weight: 400;
  }

  .close-ad {
    position: absolute;
    top: 8px;
    right: 8px;
    width: 24px;
    height: 24px;
    border: none;
    border-radius: 50%;
    background: rgba(0, 0, 0, 0.5);
    color: #e6e7eb;
    cursor: pointer;
    font-size: 16px;
    font-weight: bold;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1;
  }

  .close-ad:hover {
    background: rgba(0, 0, 0, 0.7);
    color: #fff;
  }

  .close-ad:focus-visible {
    outline: 2px solid #fff;
    outline-offset: 2px;
  }
</style>
